'use client';

import { createTheme } from '@mui/material/styles';

// Using the default MUI theme
const theme = createTheme({
  // No customizations - using default MUI theme
});

export default theme;
